## Snowflake connection (fill with your values; do NOT commit secrets)
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_ROLE=
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=
# Optional key-pair auth
SNOWFLAKE_PRIVATE_KEY_PATH=
SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=

## Postgres connection (catalog + schedules)
# Matches docker-compose.yml defaults
PGHOST=localhost
PGPORT=5432
PGDATABASE=sme
PGUSER=sme
PGPASSWORD=sme
# or use DATABASE_URL instead of fields above
DATABASE_URL=

## Web app API base
NEXT_PUBLIC_API_BASE=http://localhost:3001

## Budget guardrails
BUDGET_CAPACITY=120

## Redis connection (cache)
# Matches docker-compose.yml defaults
REDIS_HOST=localhost
REDIS_PORT=6379
# or use REDIS_URL instead of fields above
REDIS_URL=redis://localhost:6379

## OpenAI API for Natural Language to SQL
OPENAI_API_KEY=

## Stripe Configuration (Billing)
# Get these from your Stripe Dashboard
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Billing Configuration
BILLING_ENABLED=true
BILLING_PORTAL_DOMAIN=https://your-domain.com
